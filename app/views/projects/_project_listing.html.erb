<div class="span4 project-listing">
  <a href="<%= project_path(project) %>"><%= image_tag project.project_picture.url(:medium) %></a>
  <span class="project-details">
    <h5 title="<%= project.title %>"><a href="<%= project_path(project) %>"><%= truncate(project.title, length: 57) %></a></h5>
    <%= truncate(project.description, length: 200) %><br />
    0% of INR <%= project.amount_required %><br />
    <%= project.end_date.to_s(:long) %>
  </span>
  <%= link_to (button_tag 'Explore', class: 'btn btn-small btn-block btn-inverse'), project_path(project) %>
</div>
